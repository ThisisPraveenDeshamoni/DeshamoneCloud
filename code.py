import calendar

yy = 2020  
mm = 10   

# we can also take input from user. below is the method to use
# yy = int(input("Enter year: "))
# mm = int(input("Enter month: "))

print(calendar.month(yy, mm))